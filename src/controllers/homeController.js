// src/utility/middleware.js


// Utility function to send OTP
export const sendOTP = (req, res) => {
  // Implement your logic to send OTP
};

// Utility function to verify OTP
export const verifyOTP = (req, res) => {
  // Implement your logic to verify OTP
};

// Utility function to resend OTP
export const resendOTP = (req, res) => {
  // Implement your logic to resend OTP
};

// Utility function to get user's transactions
export const getTransactions = (req, res) => {
  // Implement your logic to get user's transactions
};

// Utility function to get user's balance
export const getBalance = (req, res) => {
  // Implement your logic to get user's balance
};

// Utility function to get user's notifications
export const getNotification = (req, res) => {
  // Implement your logic to get user's notifications
};

// Add more utility functions as needed

export default { sendOTP, verifyOTP, resendOTP, getTransactions, getBalance, getNotification };
